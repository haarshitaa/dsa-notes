#include <iostream>
using namespace std;

int main() {
    char arr[100]; // Declare a character array
    cin >> arr;    // Input a string into the character array
    cout << arr[2]; // Output the third character (index 2)
    return 0;
}